<resources>

    <!-- Base application theme. -->
    <style name="AppTheme_Splash_Login" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/byzantium1</item>
        <item name="colorPrimaryDark">@color/byzantium</item>
        <item name="colorAccent">@color/byzantium</item>
    </style>

    <style name="AppTheme_Search" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/byzantium1</item>
        <item name="colorPrimaryDark">@color/byzantium</item>
        <item name="colorAccent">@color/white</item>
        <item name="colorControlNormal">@color/white</item>
    </style>

    <style name="AppTheme_Dialog" parent="Theme.AppCompat.Light.Dialog">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/byzantium1</item>
        <item name="colorPrimaryDark">@color/byzantium</item>
        <item name="colorAccent">@color/byzantium</item>
    </style>

    <style name="FullscreenTheme" parent="AppTheme_Splash_Login">
        <item name="android:actionBarStyle">@style/FullscreenActionBarStyle</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
        <item name="metaButtonBarStyle">?android:attr/buttonBarStyle</item>
        <item name="metaButtonBarButtonStyle">?android:attr/buttonBarButtonStyle</item>
    </style>

    <style name="FullscreenActionBarStyle" parent="Widget.AppCompat.ActionBar">
        <item name="android:background">@color/black_overlay</item>
    </style>

</resources>
